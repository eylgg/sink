# Keep GitHub's automatic "Source code" archives to the addon files only.
# (Name a directory itself, not dir/**: the latter drops its files but leaves
# an empty directory entry in the archive. A trailing slash does nothing here.)
.gitattributes export-ignore
.gitignore export-ignore
.luacheckrc export-ignore
README.md export-ignore
.github export-ignore
scripts export-ignore
